import React, { useState } from 'react'; import { View, Text, TouchableOpacity, StyleSheet } from 'react-native'; import { useTheme } from '../../theme';
export const Tabs = ({ tabs, active, onChange }: { tabs: string[]; active: string; onChange: (t: string) => void }) => { const { palette } = useTheme(); return (<View style={s.row}>{tabs.map(t => <TouchableOpacity key={t} onPress={() => onChange(t)} style={[s.tab, active === t && { borderBottomColor: palette.primary[400], borderBottomWidth: 2 }]}><Text style={{ fontSize: 14, fontWeight: '600', color: active === t ? palette.primary[400] : palette.text.tertiary }}>{t}</Text></TouchableOpacity>)}</View>); };
export const ProgressBar = ({ progress, color, height = 8 }: { progress: number; color?: string; height?: number }) => { const { palette } = useTheme(); return (<View style={[s.pbar, { backgroundColor: palette.surface.elevated, height }]}><View style={[s.pfill, { width: `${Math.min(Math.max(progress * 100, 0), 100)}%`, backgroundColor: color || palette.primary[400], height }]} /></View>); };
export const Divider = () => { const { palette } = useTheme(); return <View style={{ height: 1, backgroundColor: palette.surface.border, marginVertical: 8 }} />; };
export const ListItem = ({ title, subtitle, left, right, onPress }: { title: string; subtitle?: string; left?: React.ReactNode; right?: React.ReactNode; onPress?: () => void }) => { const { palette } = useTheme(); return (<TouchableOpacity onPress={onPress} style={[s.li, { backgroundColor: palette.surface.card, borderColor: palette.surface.border }]}><View style={{ marginRight: 12 }}>{left}</View><View style={{ flex: 1 }}><Text style={[s.lit, { color: palette.text.primary }]}>{title}</Text>{subtitle && <Text style={[s.lis, { color: palette.text.secondary }]}>{subtitle}</Text>}</View>{right}</TouchableOpacity>); };
export const FAB = ({ icon, onPress, label }: { icon?: string; onPress?: () => void; label?: string }) => { const { palette } = useTheme(); return (<TouchableOpacity onPress={onPress} style={[s.fab, { backgroundColor: palette.primary[500] }]}><Text style={{ color: '#FFF', fontSize: 24 }}>{icon || '+'}</Text>{label && <Text style={{ color: '#FFF', fontSize: 14, fontWeight: '600', marginLeft: 8 }}>{label}</Text>}</TouchableOpacity>); };
const s = StyleSheet.create({ row: { flexDirection: 'row', borderBottomWidth: 0.5, borderBottomColor: '#2A2A3E' }, tab: { flex: 1, paddingVertical: 14, alignItems: 'center' }, pbar: { borderRadius: 99, overflow: 'hidden' }, pfill: { borderRadius: 99 }, li: { flexDirection: 'row', alignItems: 'center', padding: 16, borderRadius: 12, borderWidth: 0.5, marginBottom: 4 }, lit: { fontSize: 16, fontWeight: '600' }, lis: { fontSize: 13, marginTop: 2 }, fab: { position: 'absolute', bottom: 24, right: 24, width: 56, height: 56, borderRadius: 28, alignItems: 'center', justifyContent: 'center', flexDirection: 'row', paddingHorizontal: 16, elevation: 8, shadowColor: '#000', shadowOffset: { width: 0, height: 4 }, shadowOpacity: 0.25, shadowRadius: 8 } });