import React from 'react'; import { View, Text, TouchableOpacity, StyleSheet } from 'react-native'; import { useTheme } from '../../theme';
export const EmptyState = ({ icon = '🔍', title, message, actionLabel, onAction }: { icon?: string; title?: string; message: string; actionLabel?: string; onAction?: () => void }) => { const { palette } = useTheme(); return (<View style={s.ctr}><Text style={{ fontSize: 48 }}>{icon}</Text>{title && <Text style={[s.h, { color: palette.text.primary }]}>{title}</Text>}<Text style={[s.m, { color: palette.text.secondary }]}>{message}</Text>{actionLabel && onAction && <TouchableOpacity onPress={onAction} style={[s.btn, { backgroundColor: palette.primary[500] }]}><Text style={{ color: '#FFF', fontWeight: '600' }}>{actionLabel}</Text></TouchableOpacity>}</View>); };
export const ErrorState = ({ message = 'Something went wrong', onRetry }: { message?: string; onRetry?: () => void }) => { const { palette } = useTheme(); return (<View style={s.ctr}><Text style={{ fontSize: 48 }}>⚠️</Text><Text style={[s.m, { color: palette.text.secondary }]}>{message}</Text>{onRetry && <TouchableOpacity onPress={onRetry} style={[s.btn, { backgroundColor: palette.primary[500] }]}><Text style={{ color: '#FFF', fontWeight: '600' }}>Retry</Text></TouchableOpacity>}</View>); };
const s = StyleSheet.create({ ctr: { alignItems: 'center', paddingVertical: 60 }, h: { fontSize: 20, fontWeight: '700', marginTop: 12 }, m: { fontSize: 14, textAlign: 'center', marginTop: 8, paddingHorizontal: 32 }, btn: { marginTop: 20, paddingVertical: 12, paddingHorizontal: 24, borderRadius: 12 } });