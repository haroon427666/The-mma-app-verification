import React from 'react'; import { View, Text, TouchableOpacity, StyleSheet } from 'react-native'; import { useTheme } from '../../theme'; import { radius } from '../../tokens';
export const Badge = ({ label, color = '#3B82F6' }: { label: string; color?: string }) => (<View style={[s.badge, { backgroundColor: color + '20' }]}><Text style={[s.text, { color }]}>{label}</Text></View>);
export const Chip = ({ label, selected, onPress, color = '#3B82F6' }: { label: string; selected?: boolean; onPress?: () => void; color?: string }) => { const { palette } = useTheme(); return (<TouchableOpacity onPress={onPress} style={[s.chip, { backgroundColor: selected ? color : palette.surface.card, borderColor: selected ? color : palette.surface.border }]} accessibilityRole="button"><Text style={{ fontSize: 13, fontWeight: '600', color: selected ? '#FFF' : palette.text.secondary }}>{label}</Text></TouchableOpacity>); };
export const Avatar = ({ name, size = 40 }: { name?: string; size?: number }) => (<View style={[s.avatar, { width: size, height: size, borderRadius: size / 2, backgroundColor: '#1E1E32' }]}><Text style={{ fontSize: size / 2.5, color: '#9CA3AF' }}>{name?.charAt(0)?.toUpperCase() || '👤'}</Text></View>);
const s = StyleSheet.create({ badge: { paddingHorizontal: 8, paddingVertical: 2, borderRadius: 999 }, text: { fontSize: 11, fontWeight: '700', textTransform: 'uppercase' }, chip: { paddingHorizontal: 14, paddingVertical: 8, borderRadius: 20, borderWidth: 1 }, avatar: { alignItems: 'center', justifyContent: 'center', overflow: 'hidden' } });