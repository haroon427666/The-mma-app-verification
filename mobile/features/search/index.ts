/** Search feature barrel + Notifications + Watchlist — consolidated production modules */

// Search
export { SearchScreen, useSearch, useAutocomplete, usePopularSearches, useSearchStore, searchActions, searchKeys, searchAnalytics } from './screens/SearchScreen';
export type { SearchResult, SearchState } from './screens/SearchScreen';
